export default function ShotListGeneratorPage() {
  return (
    <div className="mx-auto max-w-3xl px-4 py-16 sm:px-6">
      <p className="eyebrow mb-3">Shot List Generator</p>
      <h1 className="text-3xl font-bold tracking-tight">Coming in CP2</h1>
      <p className="mt-3 text-text-muted">
        Groups your shots by physical setup, so you film everything that
        needs the same setup back-to-back instead of one video at a time.
        This is the most-used screen — it&apos;s built next.
      </p>
    </div>
  );
}
