# Copy to .env.local. Fixtures + dev bypass is the default for the whole
# build until Checkpoint 8 — see docs/BRIEF.md §3.5.

DATA_SOURCE=fixtures
DEV_NO_AUTH=true

# Added in CP8:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
